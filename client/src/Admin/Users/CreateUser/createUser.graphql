mutation createUserMutation(
$email: String!
$name: String!
$isEnabled: Boolean!
) {
  createUser(
    email: $email
    name: $name
    isEnabled: $isEnabled
  ) {
    id
    name
    email
    isEnabled
  }
}